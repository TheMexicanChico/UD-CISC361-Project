#include "cpu.h"

// this will be returned by the read queue function after it gets called in the submission queue
//job* jobReady;

// when reading in jobs, first one with allowed memory allocation goes right to ready queue

void get_job(job* jobReady) {
    
}
